import { ViewStyle } from "react-native";

type Props = {
  style?: ViewStyle;
};

const MenuButton = ({ style }: Props) => {
  return (
    <>
      <></>
    </>
  );
};

export default MenuButton;
